//Default Export
//You can only have one default export in a file

const details = () => {
    const name = "Rabdya Santhosh";
    const age = 21;
    const college = "OUCE";
    return name + " is " + age + "years old. He is studying in " + college;
};

export default message;
